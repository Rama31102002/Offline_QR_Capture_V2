'use strict';
async function blobToData(blob){return new Uint8Array(await blob.arrayBuffer());}async function sha256Hex(blob){const b=await crypto.subtle.digest('SHA-256',await blob.arrayBuffer());return [...new Uint8Array(b)].map(x=>x.toString(16).padStart(2,'0')).join('');}
async function exportBackup({all=false,userId=null,newOnly=false}={}){if(typeof JSZip==='undefined')throw new Error('Backup library is unavailable.');const session=currentSession();if(!session)throw new Error('Please login first.');if(all&&session.role!=='admin')throw new Error('Administrator required.');let captures=(await dbGetAll('captures')).filter(r=>!r.pseudo_deleted);if(!all)captures=captures.filter(r=>r.user_id===(userId||session.user_id));if(newOnly)captures=captures.filter(r=>r.export_status!=='exported');if(newOnly&&!captures.length)return 0;const zip=new JSZip(),meta=[];const batchId=crypto.randomUUID(),exportedAt=new Date().toISOString();for(const r of captures){const imageName=`images/${r.display_record_id||r.record_id}.jpg`;zip.file(imageName,await blobToData(await getRecordImageBlob(r)));const m={...r,image_blob:undefined,image_file:imageName};delete m.image_blob;meta.push(m);}const payload={export_version:2,application:'Offline QR Capture',app_version:APP_VERSION,db_schema_version:DB_SCHEMA_VERSION,qr_schema_version:QR_SCHEMA_VERSION,device_uuid:await getSetting('device_uuid',''),device_code:await getSetting('device_code',''),device_name:await getSetting('device_name',''),exported_at:exportedAt,export_batch_id:batchId,record_count:meta.length,scope:all?'all_pending':'user_pending',records:meta};zip.file('records.json',JSON.stringify(payload,null,2));const manifest={export_batch_id:batchId,created_at:exportedAt,record_count:meta.length,records:meta.map(r=>({record_id:r.record_id,display_record_id:r.display_record_id,image_file:r.image_file,image_sha256:r.image_sha256}))};zip.file('manifest.json',JSON.stringify(manifest,null,2));const blob=await zip.generateAsync({type:'blob',compression:'DEFLATE',compressionOptions:{level:6}});const stamp=new Date().toISOString().replace(/[:.]/g,'-').slice(0,19),dev=(await getSetting('device_code','DEVICE')).replace(/[^A-Za-z0-9_-]/g,'_');downloadBlob(blob,`Offline_QR_Export_${dev}_${stamp}.zip`);if(newOnly){for(const r of captures){r.export_status='exported';r.exported_at=exportedAt;r.export_batch_id=batchId;await dbPut('captures',r);}}await setSetting('last_backup_at',exportedAt);return captures.length;}
function downloadBlob(blob,name){const a=document.createElement('a'),url=URL.createObjectURL(blob);a.href=url;a.download=name;document.body.appendChild(a);a.click();setTimeout(()=>{URL.revokeObjectURL(url);a.remove();},1000);}
